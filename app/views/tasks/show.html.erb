<p><%= @task.title%></p>
<p><%= @task.details%></p>
<p><%= @task.completed%></p>

<%= link_to "Delete #{@task.title}",
      task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

<%= link_to "update #{@task.title}", edit_task_path(@task) %>
<%= link_to "home", tasks_path()%>